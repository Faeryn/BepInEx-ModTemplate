using BepInEx;
using BepInEx.Bootstrap;
using BepInEx.Configuration;
using BepInEx.Logging;
using Mono.Cecil;

namespace {{name}} {

	public class {{name}}Patcher {
		private static ManualLogSource Log = Logger.CreateLogSource("{{name}}");
		
		// Keep this in sync with the main plugin!
		public const string GUID = "${guid}";

		public static IEnumerable<string> TargetDLLs { get; } = new string[0];

		public static void Initialize() {
			Log.LogInfo("Initializing {{name}} patcher");
			ConfigFile config = new ConfigFile(Utility.CombinePaths(Paths.ConfigPath, GUID+".cfg"), false);
			// Define configs here
		}
	
		public static void Patch(AssemblyDefinition assembly) {
			
		}
	}
}